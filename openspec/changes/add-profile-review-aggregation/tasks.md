## 1. Walking skeleton

- [ ] 1.1 Render one Profile Aggregate from five eligible Reviews through the canonical Profile route, including one capability distribution, Run it back distribution, eligible counts, relationship context, and an **Early signal** label (test: five-review end-to-end fixture).
- [ ] 1.2 Put every Profile aggregate page and public-data read behind one versioned public projection that exposes no Account, anonymous-author, exact-time, evidence, moderation, or integrity fields (test: public/restricted field authorization matrix).
- [ ] 1.3 Connect publication, revision approval, withdrawal, limitation, removal, Relationship Claim, integrity, merge, and Profile-removal transitions to authoritative source-set invalidation (test: source event to projection-state matrix).

## 2. Eligibility and anti-domination

- [ ] 2.1 Implement current published-Review, accepted-Claim, public-Profile, supported-Assessment, and integrity eligibility with one most-recent eligible Assessment per Account/Profile (test: complete eligibility and repeat-engagement fixtures).
- [ ] 2.2 Apply five-distinct-reviewer thresholds independently to every metric, Run it back, relationship breakout, and verification breakout; exclude **Not enough exposure** (test: zero-through-six boundary table per field).
- [ ] 2.3 Add the five coarse relationship groups, diversity/concentration rules, and **Concentrated perspective** outcome without hidden group weighting (test: 5+0, 4+1, 3+2, 8+2, 9+2, and multi-group cases).

## 3. Public summaries

- [ ] 3.1 Render five-position distributions, categorical medians when eligible, exact coverage denominators, and persistent **Legit ↔ LARP** direction without decimal means (test: consensus, polarization, even/odd median, and inverse-LARP snapshots).
- [ ] 3.2 Render Run it back as its three canonical categories with its own denominator and no derived verdict (test: every categorical mix and missing-source case).
- [ ] 3.3 Show relationship and evidence-state context, explain that verification does not verify assessment truth, and label five-to-nine answers **Early signal** (test: copy and accessibility snapshots).

## 4. Privacy and integrity

- [ ] 4.1 Enforce primary and complementary suppression across Profile pages, public interfaces, metadata, exports, notifications, and future-surface fixtures (test: residual subtraction and overlapping-cell attack corpus).
- [ ] 4.2 Prohibit attribution-mode, author, exact-time, organization, project, arbitrary, and combined filters; keep named and anonymous answers behaviorally identical (test: public query allowlist and anonymous differential suite).
- [ ] 4.3 Exclude coordinated, duplicate, held, and enforcement-ineligible inputs through restricted policy signals without exposing cluster or fraud state (test: positive/negative brigading and Sybil fixtures).

## 5. Freshness, failures, and close-out

- [ ] 5.1 Version aggregation policy, Assessment scales, and source sets; serve cached output only at the current source version and invalidate subtractive changes before public visibility changes (test: concurrent publish/edit/withdraw/remove timelines).
- [ ] 5.2 Add literal not-enough-reviews, not-enough-exposure, concentrated, updating, removed, and unavailable states with individual Review fallback where allowed (test: empty/error/deleted/merged/provider-outage matrix).
- [ ] 5.3 Publish methodology covering eligibility, self-selection limits, coverage, suppression, revisions/removals, and no Profile comparison; add monitoring for privacy and manipulation regressions (test: methodology contract and policy-version audit).
- [ ] 5.4 Run unit, integration, privacy, authorization, moderation, accessibility, cache-consistency, and browser tests, then complete specialist statistical/privacy/legal review before any real-person aggregate launch.
